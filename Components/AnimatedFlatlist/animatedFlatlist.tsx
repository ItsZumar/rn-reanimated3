import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const AnimatedFlatlist = () => {
  return (
    <View>
      <Text>AnimatedFlatlist</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
